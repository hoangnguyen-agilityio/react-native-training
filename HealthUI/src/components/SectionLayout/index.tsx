import React from 'react';
import { View } from 'react-native';
import style from './style';

const SectionLayout = (props: any) => {
  return <View style={style.root} {...props} />;
};

export default SectionLayout;
