import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    padding: 17,
    borderRadius: 50,
  },
  shadow: {
    shadowColor: 'black',
    shadowOffset: {
      width: 4,
      height: 4,
    },
    shadowOpacity: 0.37,
    shadowRadius: 24,
    elevation: 20,
  },
});

export default styles;
