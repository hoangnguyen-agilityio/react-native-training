import { storiesOf } from '@storybook/react-native';
import React from 'react';
import StatusBarPage from '../../../pages/StatusBar';

storiesOf('StatusBar', module).add('default', () => <StatusBarPage />);
